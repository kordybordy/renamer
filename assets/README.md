Pixel-style logo assets for the Renamer GUI live here:

- `logo.png` / `logo.ico` – primary app icon (multi-resolution)
- `logo-32.png`, `logo-64.png`, `logo-256.png` – source renders for packaging

Note: these logo files are not included in the repository. Add your own copies with the names above before packaging or running with custom icons.
